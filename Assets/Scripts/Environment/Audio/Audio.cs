using UnityEngine;
using System.Collections;

namespace Environment.Audio
{
    public class AudioManager
    {

        // Use this for initialization
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}